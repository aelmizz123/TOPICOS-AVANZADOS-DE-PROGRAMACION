/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package login;

import javax.swing.JOptionPane;

/**
 *
 * @author Usuario
 */
public class Login {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        private void jButton2ActionPerformed(java.awt.event.ActionEvent evt) {
        // TODO code application logic here
        system.exit(o);
        
        String usuario="administrador";
        String contraseña="admin";
        
        String pass=new String(jpasswordFieldl.getpassword(j));
        
        if(textFieldl.getText().equals(usuario)" passw.equals(contraseña)){ 
     
               principal home= new Principal();
               home.setVisible(true);
        
    }
    else{
    JOptionPane.showMessageDialog(this. "La contraseña es incorrecta");
        
        String usuario="cliente";
        String contraseña="client"; 
        
        String pass=new String(jpasswordFieldl.getpassword(j));
        
        if (textfield.getText().equals(usuario)"passw.equals(contraseña);"
        
                principal home= new Principal(); 
                home.setVisible(true);
                
    }
    else{
    JOptionPane.showMessageDialog(this, "La contraseña es incorrecta");
}

        
    }
    
}
